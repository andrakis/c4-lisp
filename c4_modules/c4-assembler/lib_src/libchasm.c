/**
 *  Platform library: libchasm
 * 
 * Implements functions used by chasm, the c4 assembler.
 * Provides introspection into the C4 runtime (opcodes and such) as well
 * as functions for manipulating C4 at runtime.
 */

#include "core/c4_module.h"